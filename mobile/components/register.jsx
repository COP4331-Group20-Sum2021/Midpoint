import React from 'react';
import { Text, View } from 'react-native';

export default function Register({ navigation }) {
    return (
        <View>
            <Text>This is the register screen in "components/register.jsx"</Text>
        </View>
    )
}